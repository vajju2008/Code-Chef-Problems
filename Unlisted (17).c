#include<stdio.h>
int main()
{
	int length;
	int breadth;
	float Area;
	length=10;
	breadth=5;
	Area=length*breadth;
	printf("\n length=%d",length);
	printf("\n breadth=%d",breadth);
	printf("\n Area=%f",Area);
	return 0;
	
}