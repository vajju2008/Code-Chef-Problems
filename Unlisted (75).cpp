#include<stdio.h>
int main()
{
	int n,i,c,a[4];
	for(i=0,i<4,i++);
	{
		printf("enter no of weeks");
		scanf("%d",&a);
		if(n<10){
			printf("console");
		}
		else{
			printf("problem");
			i++;
		}
		printf("problem a weeks is %d",a);
		return 0;
	}
}
