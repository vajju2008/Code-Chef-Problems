#include <stdio.h>
int main()
{
	int n;
	printf ("Enter number of yoga poses performed:");
	scanf ("%d", &n);
	int r;
	r = n/12;
	printf ("Number of rounds completed is %d", r);
 return 0;
}

