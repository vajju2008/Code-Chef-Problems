#include<stdio.h>
int main()
{
	int i,t,s[5]={60,70,80,90,90};
	FILE *fp;
	fp =fopen("c:kuldeep.txt","r");
	printf("enter total runs ");
	fgets("runs");
	scanf("%d",&t);
	fgets(max);
	fgets(min);
	f printf(fp,%d,total runs);
	f close(fp);
}
