#include <stdio.h>
 int main () {
 	double radius = 8.9;
 	double pi = 3.14;
 	
 	double area;
 	 area = pi*radius*radius
 	 
 	 printf("radius: %f/n", radius);
 	 printf("Radius: %f/n", pi);
 	 printf("Area of the rectangle": %f/n", area);
 	 return 0
 	 
 }
 
