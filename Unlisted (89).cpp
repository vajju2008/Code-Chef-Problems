#include<stdio.h>
int sum (int);
int main()
{ int n,s=0,i;
printf("enter a number");
( "%d",&n);
for(i=0;i=n;i++)
{ 
s=sum(i);
}
int sum (int i);
int (s=0);
{
if(i!=0)
return i+sum(i-1);
}
if else;
return i ;
}
